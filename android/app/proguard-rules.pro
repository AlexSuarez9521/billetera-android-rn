-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile

# react-android ya trae sus reglas consumer para el puente (subclases de NativeModule y los
# métodos con @ReactMethod), así que no hace falta repetirlas aquí.
